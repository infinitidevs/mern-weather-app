import React from 'react';

import { WeatherExtraWrapper } from './styles';

const ExtraCardWrapper = ({ children }) => {
  return <WeatherExtraWrapper>{children}</WeatherExtraWrapper>;
};

export default ExtraCardWrapper;
